CREATE TABLE baselines ( id UUID PRIMARY KEY, user_id UUID NOT NULL, version INT NOT NULL DEFAULT 1, traits JSONB, rules JSONB, checksum TEXT, created_at TIMESTAMP WITH TIME ZONE DEFAULT now() );CREATE TABLE baselines ( id UUID PRIMARY KEY, user_id UUID NOT NULL, version INT NOTCREATE TABLE baselines ( id UUID PRIMARY KEY, user_id UUID NOT NULL, version INT NOT NULL DEFAULT 1, traits JSONB, rules JSONB, checksum TEXT, created_at TIMESTAMP WITH TIME ZONE DEFAULT now() );

# SOVV Engineering Agent

Server-side agent package for the SOVV Engineering Chat.

The agent exposes high-level engineering tools only and calls the secure Worker facade instead of calling GitHub or Cloudflare directly.

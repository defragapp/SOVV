{
  "name": "sovereign-control",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env development",
    "build:ui": "cd ../sovereign-control-ui && npm install && npm run build",
    "deploy": "npm run build:ui && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.0.0"
  }
}

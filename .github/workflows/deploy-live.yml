## DEPRECATED — DO NOT USE
## This workflow is preserved for reference only.
## Production deployment is now handled by Cloudflare Workers Builds (Git integration).
## Delete this file after Cloudflare Workers Builds is confirmed active on sovv-web.
##
## To activate Cloudflare Workers Builds:
##   1. Cloudflare Dashboard → Workers & Pages → sovv-web → Settings → Build & Deployments
##   2. Connect GitHub repo: defragapp/SOVV, branch: main
##   3. Build command: cd apps/web && npx opennextjs-cloudflare build
##   4. Output directory: apps/web/.open-next
##   5. Add env vars: JWT_SECRET, NEXT_PUBLIC_API_URL, NEXT_PUBLIC_AI_URL
##   6. Delete this file once confirmed active.

# name: Deploy to Cloudflare on main
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     env:
#       CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 24
#           cache: npm
#       - run: npm ci
#       - run: npm run build
#       - run: npm run deploy -w apps/worker
#       - run: npm run deploy -w apps/web
name: Legacy Deploy Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Legacy deploy disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy deploy workflow disabled. Use deploy-platform-cloudflare.yml instead."

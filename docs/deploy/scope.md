# Deploy scope

For web-only releases, the scope is:

- `apps/web`
- `sovv-web`

Out of scope:

- `sovereign-os-api`
- `worker-ai`
- `worker-session`
- `sovereign-control`
- developer/build/code agent Workers

Do not broaden scope without explicit approval.
